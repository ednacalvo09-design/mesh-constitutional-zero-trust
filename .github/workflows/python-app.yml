name: Mesh Constitutional - CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.11
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"
    
    - name: Install deps
      run: |
        python -m pip install --upgrade pip
        pip install pytest pylint
    
    - name: Check - arquivos com espaco
      run: |
<<<<<<< HEAD
        echo "Verificando..."
        if find src -name "* *" -type f | grep .; then
          echo "ERRO: arquivo com espaco!"
          exit 1
        fi
        echo "OK - sem espacos"
    
    - name: Lint - Pylint
      run: |
        pylint src/ --errors-only || true
    
    - name: Test - Quality Memory
      run: |
        pytest src/runtime/5-referencia-qualidade-memoria/tests/ -v || true
=======
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      env:
        PYTHONPATH: .
      run: |
        export PYTHONPATH=$PYTHONPATH:./src/runtime/3-execucao-area-operacional:./src/runtime/4-governanca-autoridade-fiscalizacao:./src/runtime/2-comunicacao-sistema-nervoso:./src/runtime/1-fundacao-alicerce:./src/runtime:./src/runtime/5-referencia-qualidade-memoria/tests:.
        python -m pytest src/runtime/5-referencia-qualidade-memoria/tests/test_exemplo.py -v
>>>>>>> 7d23160bbe3ad0ae7ca164b3b8596dd03e5f3f59
