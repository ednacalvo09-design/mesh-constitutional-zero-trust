version: '3.8'

services:
  mesh-core:
    build: .
    container_name: mesh-constitutional-zero-trust-core
    restart: unless-stopped
    volumes:
      - .:/app
    environment:
      - MESH_ENV=production
      - ZERO_TRUST_ENABLED=true
    command: python3 tests/teste_bloqueio.py
